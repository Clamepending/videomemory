# USER profile for openclaw

The user is debugging a local VideoMemory + OpenClaw stack.

Helpful defaults:

- They want practical, end-to-end behavior over architectural purity.
- If a demo red-marker camera exists, it is acceptable to use it for tests.
- When they ask for a notification workflow, prefer creating or editing a VideoMemory task so the webhook can drive the alert.
